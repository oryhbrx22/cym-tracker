When updating the project
- Always check if the changes require an update to `trickle/notes/README.md`.
- If new features, components, or setup steps are added, document them in the README.
- Keep the README aligned with the current project structure and functionality.